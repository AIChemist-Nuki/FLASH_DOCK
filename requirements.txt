# ============================================================
# FLASH_DOCK — pip layer (reference only).
#
# RECOMMENDED: use the single unified conda env instead —
#     conda env create -f environment.yml   &&   conda activate flashdock
#
# This list is just the pip-installable Streamlit packages. The full app also
# needs torch / torch_geometric / pytorch_scatter / pytorch_cluster / fpocket /
# rdkit, which on Apple Silicon must come from conda-forge (see environment.yml).
# Pokeformer pocket detection now runs in the SAME env — no separate env.
# ============================================================

# --- Web UI ---
streamlit==1.48.1
streamlit-molstar==0.4.21
streamlit-ketcher==0.0.1

# --- Molecular visualization ---
py3Dmol==2.5.2
stmol==0.0.9

# --- Cheminformatics ---
rdkit==2025.3.5

# --- Scientific stack ---
numpy==1.26.4
pandas==2.3.1
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
seaborn==0.13.2

# --- Utilities ---
tqdm==4.67.1

# --- Used by the bundled Uni-Mol docking subprocess ---
lmdb==1.7.5
biopandas==0.5.1
